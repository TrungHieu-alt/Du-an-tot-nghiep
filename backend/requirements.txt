# Production API runtime
fastapi==0.115.6
uvicorn[standard]==0.24.0
pydantic==2.11.7
psycopg[binary]==3.2.3
numpy==1.26.4
# Slice 4: multipart/form-data uploads for /api/documents.
python-multipart==0.0.20

# Slice 5: PDF text extraction for parse worker.
pypdf==4.3.1

# TestClient support
httpx==0.28.1

# Slice 8: local multilingual cross-encoder reranker.
sentence-transformers==3.0.1
